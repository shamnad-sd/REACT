import React from 'react'

const Sign-In = () => {
    const name = useRef();
    const email = useRef();
    const password = useRef();

  return (
    <div>Sign-In</div>
  )
}

export default Sign-In